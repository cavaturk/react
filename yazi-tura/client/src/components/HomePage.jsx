import React from 'react';
import { Card, Input} from 'antd';

const HomePage = () => (
  <Card className="container" title="React Application">
    <p>Content</p>
  </Card>

);

export default HomePage;